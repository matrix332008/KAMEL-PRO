name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" ${{ secrets.SUPABASE_URL }}/rest/v1/
